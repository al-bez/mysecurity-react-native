import { StyleSheet } from 'react-native'
import { Metrics, ApplicationStyles } from '../../Themes/'

const {
  screen: { container, backgroundImage }
} = ApplicationStyles

export default StyleSheet.create({
  backgroundImage,
  container
})
